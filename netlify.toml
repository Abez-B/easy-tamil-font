[build]
  command   = "npm run build"
  publish   = "dist"

# SPA fallback — send all unknown paths to index.html so React Router works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
